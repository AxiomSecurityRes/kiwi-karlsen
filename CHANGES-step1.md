# Kiwi Karlsen — Step 1: 엔진 & 게임 리뷰 파운데이션 (v29)

전체 개편(통찰 / 분석·리뷰 / 탐색기)의 **1단계**. 나머지 기능의 정확도가
모두 엔진 평가에 의존하므로, 엔진과 리뷰 파이프라인을 먼저 바로잡았다.

## 1. 최신 Stockfish 18 (NNUE) 로 교체 — 엔진 편향/부정확 해결
- 구식 클래식 Stockfish(~360KB) → **Stockfish 18 Lite NNUE** 로 교체.
  - `stockfish-18-lite-single.js/.wasm` (기본, ~7MB, CORS 헤더 불필요 → 모바일 포함 전 브라우저)
  - `stockfish-18-lite.js/.wasm` (멀티스레드, `crossOriginIsolated` 시 자동 사용)
- `engine.js` 로더 전면 재작성:
  - 실행 환경 감지 → 멀티스레드/단일스레드 자동 선택, 실패 시 단일→내장 JS 순 폴백.
  - 멀티스레드 시 `Threads`(코어 수)·`Hash` 설정. 리뷰는 워커 풀로 **국면 단위 병렬**.
  - **tempo 편향 보정 해킹(`TEMPO_CP=28`) 제거** — NNUE 는 차례 편향이 없어 이제 오차만 유발했음.
    (검증: 백 ~53 / 흑 ~47 로 대칭·안정. 옛 엔진의 ±0.5폰 톱니파 소멸)
- 백엔드 `security.py` 에 `Cross-Origin-Embedder-Policy: require-corp` 추가 →
  `crossOriginIsolated` 활성화로 멀티스레드 가속 가능(모든 리소스 same-origin 이라 안전).

## 2. 게임 리뷰 "작동 안 함" 버그 수정
- **중복 정의 제거**: `analysis.js` 에 `saveReviewToInsights` 가 두 번 정의돼 있었고,
  깨진 두 번째 정의가 첫 번째를 덮어써서 존재하지 않는 `drops` 참조 → 예외 →
  **통찰 저장이 조용히 실패**하고 있었다. 정상 버전만 남김.
- **파이프라인 견고화**: `fullReview` 를 `try/catch/finally` 로 감싸고,
  엔진이 일부 국면에서 `null` 을 반환해도 안전 기본값으로 채우도록 가드 추가.
  이전에는 여기서 예외가 나면 스피너가 멈추고 버튼이 잠겨 "작동 안 함" 으로 보였다.

## 3. 수 퀄리티 산출 정확도 향상 (Chess.com 지향)
- `review.js` 분류기 개선:
  - **절대 센티폰 손실(cpDrop) 가드**: 승률이 포화된 압승/완패 구간에서
    룩·마이너피스급 헌납이 '뛰어남'으로 잘못 찍히던 문제 해결(최소 등급 강제).
  - **'훌륭한 수'** 판정에 접전 구간 조건 추가(이미 압승/완패면 제외).
- `analysis.js` 가 mover 관점 cpDrop 을 계산해 분류기로 전달.

## 검증 (실제 Stockfish 18 로 종단 테스트)
스콜라 메이트 함정 기보로 확인:
```
1.e4  최선의 수   |  1...e5  최선의 수
3.Bc4 좋은 수     |  3...Bc5 좋은 수
5.Qh5 좋은 수(이 국면에선 정당한 함정) | 6...Nf6?? 블런더 (Qxf7# 허용, 승률 51→0)
```
MultiPV·평가·분류가 모두 정확히 동작.

## 다음 단계
- **Step 2**: 통찰(Insights) 대규모 확장 — 결과별/단계별/기물별 정확도, 수 번호별 정확도,
  게임 양상, 캐슬링, 요일·시간대, 오프닝 TOP10 등.
- **Step 3**: 탐색기 & 오프닝 — Lichess Database 로딩 실패 수정 + 오프닝 보강.
