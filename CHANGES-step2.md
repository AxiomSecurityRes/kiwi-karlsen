# Kiwi Karlsen — Step 2: 통찰 대규모 확장 + Chess.com 게임 가져오기 (v30)

Step 1(엔진·리뷰) 위에 누적된 빌드입니다. (Step 1 변경 포함)

## 1. Chess.com 게임 가져오기 (신규)
- 통찰 페이지 상단에 **Chess.com 사용자명 입력 → 최근 N개월 게임 가져오기** 카드 추가.
- 백엔드 `chesscom.py`: Chess.com 공개 API(무인증)로 월별 아카이브를 받아
  내 Game 으로 저장. 승패·오프닝·시간제어·종료유형·레이팅 추이에 **즉시 반영**.
  - 중복은 게임 URL(ext_id)로 방지, 변형 체스 제외, 최대 300판.
  - 종료 사유를 한국어로 매핑(체크메이트/기권/시간패/합의 무 …), 시간제어·레이팅·일시 파싱.
  - **내 Glicko 레이팅은 건드리지 않음** — Game 행만 삽입.
- 엔드포인트: `POST /api/insights/import/chesscom`, `GET /api/insights/import/status`.
- 가져온 게임을 분석 페이지에서 리뷰하면 정확도·수 분류 등 세부 지표까지 채워집니다.

## 2. 통찰(Insights) 세부 지표 대폭 확장
수별 상세(ReviewMove) + 리뷰 요약(GameReview)을 집계해 다음을 신규 제공:
- **결과별 평균 정확도** — 이길 때 / 비길 때 / 질 때 / 총합
- **게임 단계별 정확도** — 오프닝 / 미들게임 / 엔드게임
- **수 번호별 정확도** — 1–10 / 11–20 / … / 41수+
- **기물별 평균 정확도** · **기물별 움직임 수**
- **게임 양상** — 시종 우세 / 역전 / 리드 상실 / 시소 / 압도 / 완패 / 균형,
  양상별 승률·정확도까지 (분석 리뷰 시 승률 곡선으로 자동 판정)
- **어느 단계에서 끝났나** + 단계별 결과(승/무/패)
- **전술 포착률**(찾은/놓친/%) · **게임당 이론 수 평균**
- **캐슬링** — 킹/퀸사이드 횟수, 캐슬링한 게임 비율, 평균 캐슬링 시점

기존 지표(레이팅 추이·색깔별·상대 실력대별·시간제어·종료유형·오프닝 TOP·수 분류
분포·게임 길이·연속 기록·활동 히트맵·훈련 현황)는 그대로 유지.

## 3. 스키마 변경 (자동 마이그레이션)
- `users`: `chesscom_username`, `chesscom_synced_at`
- `games`: `source`('site'/'chesscom'), `ext_id`(중복 방지)
- `game_reviews`: `game_shape`(게임 양상)
- 기존 DB는 `_migrate_columns()`가 배포 시 ALTER TABLE 로 자동 추가(검증 완료).

## 검증
- 인메모리 DB 로 `insights.build` 종단 실행 — 모든 신규 지표 정상 산출 확인.
- Chess.com PGN 파서(결과·종료사유·시간제어·수순·일시) 오프라인 테스트 통과.
- 기존 DB 마이그레이션(새 컬럼 자동 추가) 테스트 통과.
- 전체 백엔드 컴파일 + 전 프론트엔드 JS 문법 검증 통과.

## 참고
- Chess.com 게임의 **정확도** 지표는 각 게임을 분석 페이지에서 리뷰해야 채워집니다
  (엔진 분석이 게임당 필요). 승패·오프닝 등은 가져오는 즉시 반영됩니다.
- "지리(geography)" 항목은 상대 위치 데이터가 필요해 이번 범위에서 제외했습니다
  (추후 Chess.com 프로필의 country 필드로 확장 가능).

## 다음 단계
- **Step 3**: 탐색기 & 오프닝 — Lichess Database 로딩 실패 수정 + 오프닝 보강.
