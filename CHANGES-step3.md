# Kiwi Karlsen — Step 3: 탐색기·오프닝 + 봇 대국 통찰 포함 (v31)

Step 1(엔진·리뷰) + Step 2(통찰·Chess.com) 위에 누적된 최종 빌드입니다.

## 1. Lichess Database 로딩 실패 수정 (근본 원인 해결)
**원인**: Lichess 탐색기(explorer.lichess.ovh)는 공유 IP에서 **429(레이트리밋)**가
자주 발생합니다. 기존 코드는 *어떤* 실패에도 120초 하드 쿨다운을 걸어,
한 번 실패하면 세션 내내 "로딩 실패"로 보였습니다.

**수정**:
- **`play`(UCI 수순) 파라미터로 조회** — Lichess 서버 캐시 적중률이 높아져 429 자체가 줄어듦.
- **429 전용 쿨다운 분리** (429 → 60초 / 기타 오류 → 20초). 일시적 네트워크 오류는 1회 재시도.
- **브라우저 직접 조회 폴백** — 서버 프록시가 429에 걸리면 사용자 브라우저에서
  Lichess를 직접 호출. 각자 IP를 쓰므로 공유 IP 제한을 우회합니다. (CSP에 허용 추가)
- **실패 사유를 한국어로 표시** ("요청 한도 초과(잠시 후 자동 복구)" 등) — 무의미한
  "실패" 대신 상태를 정확히 알림.
- 캐시 키를 국면 정규화(이동/수 카운터 제외)로 바꿔 캐시 적중률 향상, TTL 72시간.

## 2. 탐색기 기능 보강
- **마스터 DB 소스 추가** — Lichess Masters(2400+ OTB 명인 대국) 선택 가능.
  이제 소스는 Lichess(수억 판) / 마스터 / 이 사이트 3종.
- 수순별 점유율(share)·평균 레이팅 표시, 로컬 통계 인덱싱 개선(최근 3000판).

## 3. 봇 대국 통찰 포함 옵션 (신규)
- 기존에는 봇 대국이 **서버에 저장조차 되지 않았습니다**(localStorage로 리뷰만 가능).
  이제 봇 대국 종료 시 `source='bot'`으로 저장합니다.
- 통찰 페이지 상단에 **"봇 대국 포함" 체크박스** 추가.
  - 기본: 제외 (사람 상대 성적이 왜곡되지 않도록)
  - 체크: 봇 대국이 게임 통계 **및 리뷰 기반 세부 지표에 모두** 반영
- 봇 대국은 **사람 상대 Glicko 레이팅에 영향을 주지 않습니다** (Game 행만 저장).
- 엔드포인트: `POST /api/games/bot`, `GET /api/insights?includeBots=1`

## 검증
- 봇 포함/제외 필터 종단 테스트: 제외 3판(정확도 70) → 포함 6판(정확도 82.5),
  게임과 리뷰 양쪽 모두 정확히 필터링 확인.
- 탐색기 SAN→UCI 변환, 캐시 키 정규화, Lichess 응답 정규화 테스트 통과.
- 로컬 탐색기 인덱싱 동작 확인.
- 전체 백엔드 컴파일 + 전 프론트엔드 JS 문법 검증 통과.

## 전체 3단계 요약
| Step | 내용 |
|------|------|
| 1 (v29) | Stockfish 18 NNUE 교체(편향 제거), 게임 리뷰 버그 2건 수정, 수 퀄리티 정확도 향상 |
| 2 (v30) | 통찰 세부 지표 대폭 확장, Chess.com 게임 가져오기 |
| 3 (v31) | Lichess DB 로딩 실패 수정, 마스터 DB 추가, 봇 대국 포함 옵션 |
